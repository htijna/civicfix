{
  "name": "civic-issue-reporting-system",
  "private": true,
  "scripts": {
    "dev:ml": "cd python-ml-service && python -m uvicorn main:app --reload --port 8000",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev:ml\"",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
